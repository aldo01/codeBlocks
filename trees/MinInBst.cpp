#include<stdio.h>
#include<malloc.h>

struct node {
int data;
struct node *left;
struct node *right;
};

// finding the min in the binary search tree....

int MinBst(struct node *head){
 if(head==Null)
   return 0;


}
